[package]
name = "soul-symbolic"
version.workspace = true
edition.workspace = true
description = "Symbolic math engine: parse, simplify, diff, prove_equal"
repository = "https://github.com/CHECKUPAUTO/SoulNeural"

[dependencies]
fastrand = "2"
